module.exports = {
  darkMode: 'class', // ✅ required for Alpine.js + dark: classes
  content: [
    './templates/**/*.html',   // ✅ adjust if needed
    './static/**/*.js',
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
